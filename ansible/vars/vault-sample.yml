vault:
  users:
    untouch: []
    default:
      - id: pi
        password: $6$...
# password is generated using % mkpasswd --method=sha-512 and encrypted via % ansible-vault encrypt vars/vault.yml
# or use inline encrypted string generated by % ansible-vault encrypt_string
